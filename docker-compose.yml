services:
  server:
    build: ./server
    ports:
      - "5001:5000"
    environment:
      - DATABASE_URL=file:./dev.db
      - JWT_SECRET=test-secret


  client:
    build: ./client
    ports:
      - "3001:3000"
    depends_on:
      - server
